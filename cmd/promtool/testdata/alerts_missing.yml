# This is the rules file.

groups:
  - name: example
    rules:
      - alert: InstanceMissing
        expr: count(up) != 2
        annotations:
          summary: "An instance is not reporting data"
